---
- name: Destroy molecule containers
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Stop and remove container
      community.docker.docker_container:
        name: ubuntu
        state: absent
        auto_remove: true
